services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    build: !reset null
    ports:
      - "0.0.0.0:8080:8080"
    restart: unless-stopped

  postgres:
    restart: unless-stopped
    ports:
      - "127.0.0.1:5432:5432"

  rabbitmq:
    restart: unless-stopped
    ports: []
